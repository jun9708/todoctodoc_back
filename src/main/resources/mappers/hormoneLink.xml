<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.todoctodoc_back.terms.mapper.TermsMapper">

    <select id="selectTerms" resultType="kr.co.todoctodoc_back.terms._dto.TermsRespDTO$TermsDTO">
        SELECT * FROM terms WHERE terms_no = #{termsNo};
    </select>

</mapper>